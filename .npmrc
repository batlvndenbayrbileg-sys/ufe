auto-install-peers=true
strict-peer-dependencies=false
resolution-mode=highest
# Flat, symlink-free node_modules so Vercel's serverless function can resolve
# transitive/native deps (@prisma/client, @node-rs/argon2) at runtime.
node-linker=hoisted
